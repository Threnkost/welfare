export const withAuthProtection = (WrappedComponent) => {
    const Component = (props) => {
        return (
            <div>
                
            </div>
        );
    }

    return Component;
}